// React
import React from "react";
// Components
import AppBar from "@material-ui/core/AppBar";
import Toolbar from "@material-ui/core/Toolbar";

function NavBar() {
    return (
        <AppBar position={"fixed"}>
            <Toolbar>HIREMELATER</Toolbar>
        </AppBar>
    );
}

export default NavBar;
